---
const urls = new Map();

// TODO: make this in a database
urls.set('instagram', 'https://www.instagram.com/g.ndre');
urls.set('github', 'https://www.github.com/Andndre');
urls.set('facebook', 'https://web.facebook.com/agung.andre.75054');

const url = urls.get(Astro.params.name);

console.log(url);

if (url === undefined) return Astro.redirect('/404');

return Astro.redirect(url);
---
